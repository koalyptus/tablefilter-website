---
title: No filters
heading: 'No filters'
section: 'examples-basic'
---

<h1>{{ heading }}</h1>
<p>
    Explicitly set the <code>grid</code> option to false to prevent the filters
    to be generated by <code>TableFilter</code>.
</p>

{{> cities-table }}

{{> tablefilter-script }}

<script data-config>
    var tf = new TableFilter(document.querySelector('.cities'), {
        base_path: 'tablefilter/',
        grid: false
    });
    tf.init();
</script>

{{> code-tabs }}
